import { Button } from "@/components/ui/button";

export const SignInButton = () => {
  return (
    <Button type="button" variant="cta" className="cursor-pointer">
      <span className="relative">Get Started</span>
    </Button>
  );
};
